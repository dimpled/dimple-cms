- สร้างฐานข้อมูลและนำเข้าฐานข้อมูล
- คอนฟิกฐานข้อมูลใหม่ basic ตั้งค่าที่ config/db.php
- อัพโหลดไฟล์ขึ้น server
- หลังจากอัพโหลดไฟล์เสร็จ ทำการตั้งค่าอ่านเขียนให้โฟลเดอร์ต่อไปนี้ /web/assets,  runtime, ให้เป็น 777  ถ้ามีไฟลเดอร์ที่ใช้เก็บไฟล์อื่นๆ ที่ทำการอัพโหลดก็ให้ทำด้วย
- ทดลองเข้าใช้งาน
- เมื่อทุกอย่าง ok ให้ทำการปิดโหมดการพัฒนาด้วย เพื่อให้ไม่มีการแสดง error จากระบบออกมาให้ดู โดยคอมเม้นสองบันทันนี้ที่ไฟล์ web/index.php 
	// defined('YII_DEBUG') or define('YII_DEBUG', true);
	// defined('YII_ENV') or define('YII_ENV', 'dev’);
-โชคดีครับ